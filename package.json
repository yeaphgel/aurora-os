{
  "name": "aurora-os",
  "version": "0.5.2",
  "private": true,
  "type": "module",
  "scripts": {
    "aurora:version": "node scripts/version.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "validate": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/node": "^22.15.17",
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  }
}
